# Pinned to what resolves as of 2026-08.
# wordfreq ships ~57 MB of bundled frequency data; simplemma lazily loads one
# dictionary per active language. Run a single worker: several multiply this.
Flask==3.1.3
requests==2.33.1
gunicorn==23.0.0
wordfreq==3.1.1
simplemma==2.0.0
